{
  "name": "42-waschingmachine-root",
  "private": true,
  "scripts": {
    "start": "docker compose -f docker-compose.yml up --build -d",
    "dev": "docker compose -f docker-compose.dev.yml up --build"
  }
}

